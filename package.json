// Please add node_modules folder to .gitignore file and avoid commiting it to the repo since it very heavy operation for git
{
  "name": "rest-api",
  "version": "1.0.0",
  "description": "todo-app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.12"
  }
}
